import React from 'react';
import useTitle from '../../../CustomHooks/useTitle';

const Home = () => {
      // Title
  useTitle("Home");
    return (
        <div>

        </div>
    );
};

export default Home;